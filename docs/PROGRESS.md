# 项目进度报告

> 对应计划：图表修复、功能完善、GitHub 开源与部署/视频方案
> 报告随各子任务完成逐节追加。

## 任务 1：图表标题完整显示 + 美化（已完成）

**完成内容**

- `bmon/charts.py` 重写标题折行逻辑：
  - 新增 `_wrap_lines` 基础折行函数；`_wrap_title` 改为"完整保留全部文字、绝不截断"，
    行宽按 30 → 36 → 42 → 50 → 60 自适应放宽，确保任意长度标题最多 3 行完整显示
  - 删除原有 "…" 截断逻辑
- Top 横向榜（`_top`）美化：
  - 标签字号随最大折行数自适应（9 / 8.4 / 7.8），避免多行标签互相挤压
  - 数值标签智能定位：长条放条内（白色加粗），短条放条外；前三名数值金色加粗强调
  - 条形左侧新增淡灰名次序号（01、02、03…）
  - 隐藏 x 轴刻度，仅保留淡虚线网格；去掉上/右/下边框，视觉更干净
  - 图例改为横排、无边框，位于右下角空白区
- 布局调整：四宫格仪表盘 figsize (17, 11) → (17.5, 13)，
  单张 top 图 (15, 9) → (15, 10)，给 Top 榜更多纵向空间

**改动文件**

- `bmon/charts.py`

**验证结果**

- `python main.py chart --period both --type dashboard` 成功生成
  `dashboard_weekly_2026-W34.png` / `dashboard_monthly_2026-08.png`
- `python main.py chart --period weekly --type top` 成功生成 `top_weekly_2026-W34.png`
- 人工检查 PNG：长标题（如「走近星穹——远坂凛：如何在宇宙时代学会智能机」）
  完整折两行显示、无截断；游戏仅以颜色区分、图例清晰；名次与数值标签正常

**产物路径**

- `output/charts/dashboard_weekly_2026-W34.png`
- `output/charts/dashboard_monthly_2026-08.png`
- `output/charts/top_weekly_2026-W34.png`

## 任务 2：快照功能验证与润色（已完成）

**现状说明**

快照功能在代码中已实现（CLI `snapshot` 子命令 + Web 页 `/snapshot`），
本轮完成验证与小润色，并补入 README（任务 4 一并处理）。

**验证结果（CLI）**

- 库内快照数据：440 条，时间范围 2026-08-16 04:41 ~ 2026-08-20 21:30
- 时刻查询：`python main.py snapshot --at "2026-08-19 21:00"` 正常返回各视频该时刻播放量
- 时段查询：`python main.py snapshot --from "2026-08-18 00:00" --to "2026-08-20 21:00"`
  正常返回期初/期末/增量三列，增量排序正确
- CSV 导出：`--csv` 导出 utf-8-sig 格式正常，标题完整无截断

**润色改动**

- `static/style.css`：`.t-title` 补充 `white-space: normal; line-height: 1.5; word-break: break-all;`，
  网页表格长标题自动折行完整显示，不产生截断观感

**Web 页验证**

`/snapshot` 页面（时刻/时段切换、筛选、CSV 导出）随任务 3 一并浏览器逐页点检，结果见任务 3 节。

## 任务 3：Web GUI 验证与润色（已完成）

**现状说明**

Web GUI 已实现（`python main.py gui`，Flask，仅监听 127.0.0.1），
含总览 / 视频数据 / 播放趋势 / 快照查询 / 运行控制五页，
采集操作通过子进程调用 main.py，与 CLI 行为一致。
本轮经浏览器逐页点检全部通过，无需代码修复；README 补充 GUI 章节见任务 4。

**浏览器逐页点检结果（全部 HTTP 200，无 JS 报错）**

- 总览页 `/`：统计卡片（183 视频 / 5.02 亿累计播放 / +197.3 万增量）、
  账号概况、 6 张图表 PNG 全部正常加载
- 视频数据 `/videos`：表格分页正常（183 条/7 页）；关键词 "PV" 筛选生效（183 → 66 条）
- 播放趋势 `/trend`：视频下拉 183 项，ECharts 折线图随选择正常更新，
  `/api/trend` 返回 200
- 快照查询 `/snapshot`：指定时刻查询返回 183 行数据；时段对比模式正确显示
  期初/期末/增量列，增量降序排列正确
- 运行控制 `/control`：任务状态、三个手动操作按钮（立即采集/重建图表/深度回填）、
  日志尾部 40 行均正常渲染（验证过程未点击任何执行按钮）

**功能覆盖确认**

GUI 已覆盖"脚本抓取 + 数据查看"全部诉求：
采集控制（fetch / fetch --full / chart 重建）+ 总览 + 筛选列表 + 趋势 + 快照 + CSV 导出。

**验证截图**

- `output/verify_1_overview.png`（总览页）
- `output/verify_2_videos_filtered_PV.png`（视频筛选页）

## 任务 4：GitHub 开源（已完成）

**完成内容**

- `git init`（main 分支）并完成首个提交（28 个文件，3702 行）
- 公开仓库已创建并推送：**https://github.com/kyo-zzz/bilibili-monitor**

**安全与文件处理**

- `.gitignore` 追加：`config.yaml`（含 cookie 等敏感字段）、`运行截图.png`（无关截图）
- 新增 `config.example.yaml` 配置模板（头部注明"复制为 config.yaml"），README 同步更新首次使用步骤
- 新增 MIT `LICENSE`（© kyo-zzz）
- 提交前经 `git status` 核验：`data/`、`.venv/`、`output/`、`config.yaml` 均未入库

**README 更新**

- 新增「本地 Web GUI」「快照查询」两个章节，命令一览表补 `snapshot` / `gui`
- 项目结构补 `webui.py`、`templates/`、`static/`、`docs/`
- 新增 Web GUI 截图两张（`docs/screenshots/webui_overview.png`、`webui_videos.png`）并在 README 嵌入
- 末尾新增许可证节

**验证结果**

- `gh api` 确认仓库 visibility=public、default_branch=main
- 远程文件清单确认无敏感文件：仅 bmon/templates/static/docs/config.example.yaml 等

## 任务 5：网页部署方案（方案稿已交付，待审阅）

**交付物**：`docs/deploy-plan.md`（仅方案，未实施）

- 核心约束分析：B站风控封杀数据中心 IP → 采集必须留在本地，云端只做展示
- 方案 A（推荐）：本地采集 + 静态报告发布到 GitHub Pages / Cloudflare Pages，零成本零风控风险
- 方案 B：内网穿透（Cloudflare Tunnel / Tailscale / frp）远程直连本机 Web GUI，数据不出本机、改动最小
- 方案 C：云服务器 Flask+gunicorn+nginx，附风控风险与成本分析，不推荐作主方案
- 含三方案对比表、推荐结论与待确认问题清单

## 任务 6：自动视频生成方案（方案稿已交付，待审阅）

**交付物**：`docs/video-gen-plan.md`（仅方案，未实施）

- 视频形态：30~60 秒 1080p 数据周报短片（片头→总览数字→图表入场→Top榜→片尾）
- 方案 A（推荐）：新增 `bmon/video.py` + `main.py video` 子命令，
  moviepy/Pillow 合成，数据与图表同源，纯 Python 无网络依赖
- 方案 B：Remotion（React 程序化视频），动效上限高，作为二期升级路线
- 方案 C：纯 FFmpeg 幻灯片，可作快速原型兜底
- 含自动投稿 B站的可行性说明与风控/登录态风险提示（建议人工投稿）

## 总结与后续建议

**本轮全部六项任务完成：**

| # | 任务 | 状态 |
|---|---|---|
| 1 | 图表标题完整显示 + 美化 | 已完成并验证 |
| 2 | 快照功能验证与润色 | 已完成（CLI + Web 双入口均验证通过） |
| 3 | Web GUI 验证与润色 | 已完成（五页浏览器点检全部通过） |
| 4 | GitHub 开源 | 已完成：https://github.com/kyo-zzz/bilibili-monitor |
| 5 | 网页部署方案 | 方案稿已交付（docs/deploy-plan.md），待审阅 |
| 6 | 自动视频生成方案 | 方案稿已交付（docs/video-gen-plan.md），待审阅 |

**后续建议（按优先级）**

1. 审阅 `docs/deploy-plan.md` 与 `docs/video-gen-plan.md`，确认选型后进入实施
2. 部署建议先启用方案 B（内网穿透，当天可用），二期再上静态公开页
3. 视频生成一期用 moviepy，数据积累越多（快照历史越长）周报内容越丰富
4. 快照数据从 2026-08-16 开始积累，建议保持每日 21:30 计划任务持续运行，
   两周后快照查询/增量对比的价值会显著提升

---

# 第二轮（2026-08-21 晚）：一键启动 / 定时采集 / 视频生成 / 部署拍板

## A：GUI 一键启动脚本（已完成）

- 新增 `open_gui.bat`：双击即启动 Web GUI 并自动打开浏览器
  （自动检测 venv；控制台窗口保持运行，关窗即停服务与调度）

## B：定时采集调度器，Web GUI 可配置（已完成）

- 新增 `bmon/scheduler.py`：计划存 `data/schedule.json`，支持
  ① 每日固定时间点（可多个） ② 时段内按间隔采集；调度循环每 20 秒重读计划，
  修改后无需重启即生效；两次采集最小间隔 10 分钟防重叠；
  采集以子进程调 `main.py fetch`，与手动按钮行为一致，日志入 `data/scheduled.log`
- Web GUI「运行控制」页新增"定时采集计划"卡片：启用开关、每日时间点、
  间隔分钟数、时段起止，保存时校验格式并展示接下来 4 次触发时间
- `main.py scheduler` 独立常驻命令（不开 GUI 也能调度，读同一份计划）
- 注：原 Windows 计划任务 BiliMonDailyFetch（21:30）与新调度器可能重复触发，
  建议删除旧任务：`schtasks /Delete /TN BiliMonDailyFetch /F`

## C：数据变化可视化视频（已完成，第一期已产出）

- 新增 `bmon/video.py` + `main.py video` 子命令（`--days` / `--from --to` / `--fps`）
- 五场景动效：片头 → 总览数字滚动 → Top8 播放趋势曲线随时间生长 →
  本期增量 Top10 条形动画（前三名金色）→ 片尾；深色主题与 Web GUI 同源
- 依赖：新增 `imageio-ffmpeg`（自带 ffmpeg 编码器，无需系统安装），已入 requirements
- 第一期产出：`output/videos/report_20260816-20260820.mp4`
  （1920×1080 / 30fps / 30 秒 / H.264，渲染耗时约 1 分钟）
- 关键帧抽检：片头/总览/趋势/条形/片尾五场景画面均正常，待用户反馈后迭代

## D：网页部署选型拍板（已完成）

- 约束：无自有云服务器、不接受复杂网络配置、主要本地查看
- **选定方案 B（Tailscale 虚拟局域网）**：无服务器/无端口映射/零公网暴露，
  启用步骤已写入 `docs/deploy-plan.md` 审定结论节
- 静态公开页（原方案 A，可分享给他人）转入下方未来任务清单（低优先级）

## 未来任务清单

| 任务 | 说明 | 优先级 |
|---|---|---|
| 视频内容与格式精调 | 数据积累更多后，按用户反馈调整视频展示内容/版式/动效细节（一期已交付，大方向符合要求） | 待数据积累 |
| 可分享的公开静态页 | 方案 A：本地采集 + 静态报告自动发布 GitHub Pages，供他人查看 | 低 |
| Tailscale 启用 | 部署已拍板方案 B，用户自行安装客户端登录后即可跨设备访问 GUI | 按需 |

---

# 第三轮（2026-08-21 夜）：WebUI 细节打磨与日志增强

## 1：新增每日(daily)粒度图表（已完成）

- `charts.build_periods` 支持 daily（按自然日划分，标签 MM-DD）；周/月/日共用四宫格仪表盘
- `chart --period` 新增 `daily` 与 `all`(日+周+月)；GUI"重建图表"按钮改为 `--period all`
- 配置默认 `periods: [daily, weekly, monthly]`（config.py 模板 / config.example.yaml / 本机 config.yaml 同步）
- 验证：`dashboard_daily_08-21.png` 生成成功（最近 12 天）

## 2：趋势页下拉按发布时间排序（已完成）

- `/trend` 下拉选项改为按 `created_ts` 降序（最新在前），选项前显示发布日期，
  默认选中仍为播放量最高视频；验证前 4 项均为最新发布顺序

## 3：定时采集表单重构（已完成）

- 两种方式拆为两行、各自独立勾选启用：
  方式一·每日固定时间点（多个时间点英文逗号隔开，UI 已备注）；
  方式二·时段内按间隔采集（每 N 分钟 + 起止时段）
- `schedule.json` 新格式 `{times_enabled, times, interval_enabled, interval_minutes, window_*}`，
  兼容旧格式自动迁移；两种都允许关闭（等同停用定时采集）
- 校验分方式独立：启用时间点但未填→报错；启用间隔但间隔为0/时段倒置→报错
- 验证：双开保存后"接下来触发"正确交错预测（20:00间隔 · 21:30定时 · 22:00间隔 · 次日 08:00）

## 4：运行日志详细化（已完成）

- `monitor.run_once` 新增轮次总结日志：耗时、视频总数(新增+N)、快照总数(新增+N)、
  各账号快照条数明细，一行即可确认本轮运行状况
- 控制页日志尾部 40 → 80 行；"最近一轮采集"卡片新增各账号快照条数明细

## 5：视频标题去游戏名前缀（已完成）

- `video.py` 新增 `_strip_game`：去除标题开头《游戏名》前缀，
  趋势场景右侧标签与增量榜标题均已应用；已重渲染 `report_20260816-20260821.mp4` 抽帧核验

## 6：部署确认

- 用户确认采用方案 B（Tailscale），启用步骤见 `docs/deploy-plan.md`

---

# 第四轮（2026-08-21 夜）：绝区零数据补采与视频动效改造

## 1：今日绝区零快照缺失排查与补采（已完成）

- 排查：按账号×日期统计快照数，确认 08-21 绝区零 0 条；日志回溯定位为
  18:59 采集进程在轮到绝区零前被意外中断（测试时误杀 python 进程），非图表聚合 bug
- 处理：手动补采一轮（feed 通道自动容错），绝区零补入 12 条快照；
  采集完成后日报/周报/月报自动重建，08-21 绝区零新增播放 20.8 万柱条已正常显示
- 附带验证：新轮次总结日志首次实际运行生效（耗时529s | 快照+39 | 各账号明细）

## 2：视频动效改造（已完成并重渲染验证）

- **折线修复（关键 bug）**：原趋势场景选片按"累计播放 Top8"，选中均为监测前
  发布的老视频（active_days=45 不再采集其指标），只有首日基线一个点，折线退化为散点。
  选片改为"期内有增长(growth>0)的视频按期末播放取 Top8"，折线完整生长
- 折线视觉增强：线宽 2.2→3.0，每个快照点带圆形标记（描边底色）
- **增量榜改为"逐日条形竞跑"**：预计算时间轴（快照序列线性插值），
  条形长度=截至当前日期的累计增量，排名随每日变化实时重排（纵向位置指数平滑过渡），
  右侧大号日期水印随扫描推进，直观展示"按每天的播放变化"
- 产出：`output/videos/report_20260816-20260821.mp4`（含绝区零今日数据），
  抽帧核验折线/竞跑两场景均正常

---

# 第五轮（2026-08-21 深夜）：视频内容扩展与设计精修

## 1：总览场景增加分游戏明细（已完成）

- 合计三卡片（监测视频/累计播放/本期增量）下方新增三行分游戏面板：
  游戏名(色点) + 视频数 + 累计播放 + 本期增量，全部数字滚动动画

## 2：新增"净增量走势"场景（已完成）

- 播放量走势之后新增一页：Top8 视频各自净增量细线 + 三个游戏合计增量粗线
  （按时间轴采样插值汇总），右侧卡片按合计增量排序并数字滚动
- 场景结构变为六段：片头→总览→播放量走势→净增量走势→逐日竞跑→片尾（约 42 秒）

## 3：逐日竞跑场景改造（已完成）

- 底部新增日期横坐标轴（整周刻度），青色进度线+日期标记随扫描推进
- 每条右侧新增"当前播放量"列（增量条旁另列总播放，一目了然）；条尾保留 +增量标注
- 主体左移（条形起点 0.34→0.28），标题字号 11.5→10，排名与视觉更紧凑

## 4：整体设计精修（已完成）

- 配色：背景/面板/边框/网格层次重新调和，金色强调改柔和琥珀色，增量统一绿色系
- 版式：所有场景统一页头（标题+青色短线+副标题+右上角时期），图表区加圆角面板
- 趋势场景纵轴不再从 0 起：按数据范围自动收缩上下界，视频间差异更清晰
- 产出：`output/videos/report_20260816-20260821.mp4`（1920×1080/30fps/42.5s），
  五场景抽帧质检均正常

---

# 第六轮（2026-08-22 凌晨）：净增量拆两幕 + 竞跑爆条 bug 修复

## 1：净增量拆分为两幕（已完成）

- 原"净增量走势"合并幕拆为：
  第 4 幕·净增量走势/视频（Top8 视频各自增量曲线 + 右侧增量标注）、
  第 5 幕·净增量走势/分游戏（三游戏合计增量粗线 + 排序卡片）
- 场景结构变为七段，总时长 49.5 秒（1485 帧）

## 2：竞跑场景爆条 bug 修复 + 播放量横轴（已完成）

- **根因**：条形绘制时误用"绝对播放量"作分子、"最大增量"作分母，导致全部条形溢出屏幕
- 重做为"播放量竞跑"：条形长度=当前播放量，**横轴=播放量**（顶部刻度+竖向网格），
  最高者占满、其余等比缩放；条尾保留"+当日新增"（为 0 时隐藏），右侧列保留精确播放量
- **截断轴自适应**（`_bar_axis`）：当各视频播放量相对差异不足最高值 45% 时
  （如全部集中在 400~500 万），自动把轴起点抬升到合适整数位（"//"标记+文字提示），
  放大差异；差异足够时从 0 起保持直观等比
- 抽帧核验：条形长度与播放量严格成比例（372 万占满→68 万≈五分之一），
  排名重排/日期扫描/增量标注均正常

## 3：分镜文档同步更新（已完成）

- `docs/video-storyboard.md` 更新为七幕结构，全部函数行号与参数位置重新校准，
  新增截断轴阈值（0.45）等调参说明

---

# 第七轮（2026-08-22 凌晨）：时间轴、平滑折线与结尾定格

## 1：总览场景新增底部时间轴（已完成）

- 整周日期刻度 + 青色进度线/圆点/当前日期标签，与数字滚动进度同步向右推进，
  与数据增长可视化呼应

## 2：折线平滑生长（已完成）

- 播放量走势 / 净增量走势·视频两幕：折线改为快照点间**线性插值采样**
  （每条线 121 个采样点），随扫描线连续平滑生长，不再等下一个快照点才跳出；
  真实快照位置仍保留圆形标记
- 净增量走势·分游戏的合计曲线本就基于 121 点采样，同样平滑

## 3：图表幕结尾定格 3 秒（已完成）

- 播放量走势 / 净增量两幕扫描完成后定格帧数 30 → 90（1 秒→ 3 秒）；
  播放量竞跑结尾定格 0.4 秒 → 3 秒（`hold_end=90`），避免切换太快影响观感

## 4：验证与文档

- 重渲染 1485 帧并抽帧核验：总览时间轴推进、趋势曲线平滑、结尾定格均正常
- `docs/video-storyboard.md` 行号与动效描述同步更新（定格参数、插值采样说明）

---

# 第八轮（2026-08-22 凌晨）：视频标题两行完整显示

## 标题不再截断（已完成）

- 原因：视频内标题统一用 `_short` 截断到 12 字 + "…"，长标题大量缺失
- 方案：新增 `_wrap2`（复用图表模块思路）：按显示宽度折行，最多两行完整显示、
  不截断；超长标题自动放宽行宽重折，宁可宽一点也不丢字；仍先去除《游戏名》前缀
- 应用位置：播放量走势/净增量·视频右侧图例槽（宽度 24、字号 9.5）、
  播放量竞跑条形左侧标题（宽度 30、字号 8.8）
- 抽帧核验：如"4.5版本「挥掷千星的筹码」前瞻特别节目"、
  "走近星穹——「知更鸟·情歌：快乐的小鸟》"均两行完整显示，无省略号
- 分镜文档同步更新折行参数说明

---

# 第九轮（2026-08-22 凌晨）：标签跟随折线 + 连续跳字 + 合计曲线去点串

## 1：右侧标签与折线一一对应（已完成）

- 新增 `_side_timeline` 预计算时间轴：右侧标签的纵向位置**跟随各自折线的当前端点**，
  与左边折线一一对应；标签互相防重叠（最小间距），帧间指数平滑过渡；
  折线交叉/超越时标签也随之平滑换位
- 应用于播放量走势与净增量·视频两幕

## 2：右侧数值连续跳字（已完成）

- 原来数值只在快照点跳变；现改为基于扫描时刻的**插值连续变化**，
  与总览场景的数字滚动观感一致（播放量走势/净增量两幕均适用）

## 3：分游戏合计曲线去"点串"（已完成）

- 原因：合计曲线基于 121 个插值采样点绘制时给每个采样点都加了圆形标记，
  视觉上退化成点串
- 修复：移除密集标记，保持 lw=3.4 平滑曲线，仅在线端保留一个圆点指示当前位置

## 4：验证与文档

- 抽帧核验：扫描中段标签与折线端点对齐、数值为中间值（跳字生效）；
  分游戏幕为平滑曲线无点串
- 分镜文档行号全部重新校准，新增标签跟随参数说明（gap/平滑系数）

---

# 第十轮（2026-08-24）：稳定性加固 + 视频报告GUI + 图表分区归档

## 1：P0-1 采集跨进程互斥锁（已完成）

- 新增 `bmon/lock.py`：`Monitor.run_once` 入口处以 O_EXCL 创建 `data/fetch.lock`
  （内容 pid+时间戳，TTL 30 分钟自动失效），finally 释放
- 效果：Windows 计划任务 BiliMonDailyFetch、GUI 内置调度器、`main.py scheduler`、
  手动 CLI 四套触发机制并存时，同一时刻只有一轮采集真正执行，
  其余秒级跳过并记录日志——彻底消除 21:30 双轨重复触发竞态
- 实测：并发两个 `main.py fetch`，第二个立即输出
  "另一采集进程正在运行(fetch.lock), 本轮跳过" 并正常退出

## 2：P1 调度器与数据库健壮性（已完成）

- 调度器 `tick`：① 时间点模式新增**错过补跑**（3 小时窗口内启动即补触发）；
  ② 间隔模式的"最近采集"改读 `last_collection()`（取调度器状态与
  monitor `state.json` 的较大者），跨进程感知手动/计划任务采集
- SQLite：连接时启用 `PRAGMA journal_mode=WAL` + `busy_timeout=5000`，
  GUI 读与采集写并发不再互相阻塞；`run_once` 账号循环补捕获
  `sqlite3.OperationalError` 与未预期异常（单账号故障不再中断整轮）

## 3：P2 工程化（已完成）

- **自动备份**：新增 `bmon/backup.py`（sqlite backup API，WAL 下快照一致）；
  每轮采集有新数据时自动备份到 `data/backup/`，保留最近 `backup_keep` 份（默认5，0=关闭）；
  另有 `main.py backup [--keep N]` 手动命令
- **日志轮转**：`setup_logging` 改用 RotatingFileHandler（2MB×3）
- **依赖钉版**：requirements.txt 按实测版本收紧下限
- **单元测试**：新增 `tests/test_core.py`（13 例：快照期初/期末/增量语义、
  筛选排序、图表折行不丢字、视频 `_wrap2/_bar_axis/_interp_value`、
  调度计划校验、互斥锁互斥与 TTL 过期），全部通过，无网络依赖
- **CI**：`.github/workflows/ci.yml`（push/PR 触发：compileall + pytest）

## 4：视频报告 Web GUI 页（已完成）

- Web GUI 新增「视频报告」页（`/video`）：
  参数表单（全部历史 / 最近N天 / 自定义时段 + 帧率 24/30/60）→
  后台子进程渲染（约 1-2 分钟，页面 8 秒自刷显示渲染状态）→
  历史视频列表（内嵌播放器预览、支持进度条拖动的 206 Range 响应、下载链接、按时间倒序）
- 子进程启动统一为 `_spawn()`（fetch/chart/video 三类，各自防重复触发）

## 5：图表按周期分区归档（已完成）

- `charts._save` 改为写入 `output/charts/{daily,weekly,monthly}/` 子目录
- `scan_chart_groups()`：扫描分组 + **旧版平铺图自动迁移**进对应子目录
- `index.html` 索引页按 日/周/月 分区展示（带张数标记）；
  Web GUI 总览页同步分区展示，不再混作一堆
- 实测：`chart --period all` 后旧图全部自动归类，新图按目录落盘

## 6：验证汇总

- pytest 13/13 通过；compileall 通过
- 真实并发锁验证通过；GUI 六页全部 200；视频页渲染-预览-下载全链路通过
  （新产出 `report_20260820-20260823.mp4`）；WAL 确认生效

---

# 第十一轮（2026-08-30）：视觉全面重设计 —— 纸感平设 × 静止系MAD

## 1：Web UI 全新设计系统 "Paper Editorial"（已完成）

- 应用户要求弃用深色与红蓝渐变，重写 `static/style.css`（v4）：
  **米白纸感底色 + 平涂流行色块（珊瑚 #ff5a5a / 奶油 #f5c542 / 雾紫 #8f7bff / 薄荷 #2fbf71）
  + 墨色文字**，完全去渐变化
- 静止系MAD图层：每页超大描边英文底字（OVERVIEW/ARCHIVE/TRENDING/SNAPSHOT/CINEMA/CONTROL）、
  顶部 mono 走带 ticker（无缝循环）、右上环形文字轨道（缓慢旋转）、胶片颗粒、
  十字准星散点、右缘竖排注释
- 编辑部式细节：统计卡自动编号 01-04 + 平涂顶色条 + 平色几何徽标、
  账号卡幽灵描边编号、图表注记 `//` 前缀、墨色反白主按钮（hover 珊瑚硬阴影）、
  active 导航墨块、mode 开关墨色反白
- 背景柔色斑（奶油/雾紫/薄荷）缓漂移 + 网格点阵；`prefers-reduced-motion` 全局尊重
- 功能零变更：全部类名保持兼容，六页结构不动

## 2：流体几何风数据视频 `bmon/video_fluid.py`（已完成，两版实渲染验证）

- 与经典版共享数据聚合与时间轴算法（collect/_side_timeline/_race_timeline 等直接复用），
  classic 渲染器原样保留，`--style classic` / 配置 `video.style` / GUI 下拉三处可切换
- 浅色平设视觉：纸感底 + 柔色斑漂移、描边底字、平涂色块、环形文字、辉光曲线、
  圆角平涂条形竞跑、mono 注记、星星点缀
- 七幕 49.5s：片头 → 总览(数字滚动/分游戏占比条/时间轴) → 播放量走势 →
  净增量·视频 → 净增量·分游戏 → 播放量竞跑 → 片尾
- 输出命名 `report_fluid_*.mp4`（与 classic 文件互不覆盖，已存在自动递增后缀）
- 修字型问题：mono 家族仅用于纯 ASCII（日期/编号），中文数字全部回落雅黑，无豆腐块
- 修布局问题：DELTA 卡片补 "+" 前缀；总览/竞跑日期轴首末点防重叠标注策略；
  分游戏行幽灵编号移入面板内
- 产出：`output/videos/report_fluid_20260825-20260829.mp4`（首版）与
  `_2.mp4`（修正版，1920×1080/30fps/49.5s）；七幕定帧展示图存于
  `docs/screenshots/video_fluid/`

## 3：展示图（docs/screenshots/）

- `fluid_webui_overview/videos/trend/snapshot/video/control.png`（六页全页截图，浅色新版）
- `video_fluid/scene1~7.png`（视频七幕定帧）
- 旧版深色截图、`webui_overview/webui_videos.png` 与经典视频文件全部原样保留

---

# 第十二轮（2026-08-30）：滚动bug修复 + 补采增强 + 图表插值估算

## 1：Web UI 无法滚动 bug 修复（P0，已完成）

- 根因：上轮为容纳描边底字给 `.content` 加了 `overflow:hidden`，配合 `html,body{height:100%}`
  使主区被裁剪为视口高度且不可滚动；此前截图验证均裁在 900px 内未暴露
- 修复：移除该 `overflow:hidden` 与固定高度，`body{overflow-x:hidden}` 兜住装饰性横向溢出；
  以 Playwright 实测滚动恢复（文档高 13907px，scrollY 可达 13007），六页重截全页截图

## 2：数据完整性审计与补采增强（已完成）

- 审计：08-17 与 08-28 两天无快照（21:30 时电脑关机/睡眠）；08-28 的 Windows 补跑
  落在 08-29 01:28（快照记入实际执行时刻），其余 12 天均准点
- 调度器补采放宽：当天时间点已过但当日尚无任何采集 → GUI/scheduler 进程随时启动
  即补触发（原限 3 小时窗口）；隔夜缺失仍由 Windows 计划任务"错过补跑"兜底

## 3：图表插值估算（新功能，仅展示不入库）

- 新增 `bmon/interp.py::estimate_at`：目标时刻落在两快照间→线性插值；
  超出末快照（如当日未采集）→末段斜率外推（负斜率钳为 0，视界 7 天外持有末值）
- `charts.agg_gains` 接入：期末值可由估算获得（基线仍取期前真实快照），
  返回 `estimated` 标记；日报/周报/月报当前期因此不再显示为 0/偏低
- 标注透明：估算参与时总标题追加"含未采集时段插值估算"，单图追加"（含插值估算）"
- 配置 `charts.interpolate`（默认开）；估算结果仅用于展示聚合，绝不写入 snapshots 表
- 测试：estimate_at 插值/外推钳位/视界 + agg_gains 开关对比，15/15 通过

---

# 第十三轮（2026-08-30 晚）：数据中心页 + 视频参数全面开放

## 1：Web UI 新增「数据中心」页（/data，已完成）

- **采集总览**：快照总量/轮次/覆盖范围/最近采集四卡；采集缺口警示条
  （自动列出无快照日期并说明插值补形策略）；每日采集分布条形图
  （条高=快照数, 顶部=当日轮次, 悬停显示明细）；账号数据分布、采集来源构成
  （detail/list）、最近 30 轮每轮快照条数列表
- **数据分析**（同页, 粒度切换 按日/按周/按月 × 最近 12 期）：
  各期播放增量表（含 CSS 比例条, 按账号分列, 复用 agg_gains 含插值标记）、
  区间播放增长 Top10（链到趋势页）、发布时间小时分布图、星期分布条
- 纯服务端计算渲染, 无需前端图表库

## 2：视频生成参数全面开放（已完成）

- `video_fluid.make_video` 新增 opts：scene_seconds(七幕各自时长, 0 跳过)、
  sweep_frac(动画推进占比 0.4~0.95)、trend_top/race_top(走势/竞跑收录数)、
  title/subtitle(片头主副标题文案)；扫描定格帧、竞跑 hold、右侧标签时间轴
  全部随参数联动(video.py `_side_timeline` 加 side_pad 参数, classic 默认值不变)
- CLI：`--scene 名称=秒数`(可多次) / `--sweep-frac` / `--trend-top` / `--race-top`
  / `--vtitle` / `--vsubtitle`；classic 风格传入时提示忽略
- GUI 视频页表单分区：内容范围 / 基础参数(风格+帧率) / 场景与动效
  (推进占比预设 + 七幕时长 + 收录数 + 文案), 留空即用默认

## 3：横向溢出根治（已完成）

- `.chart-list img` 补 `max-width:100%`：其固有宽(2450px)此前经 flex
  `min-width:auto` 链把 .content 撑到 1282px, 致全站文档宽 1498
- `.content` 加 `min-width:0` + `overflow-x:clip`（只裁横向, 纵向滚动不受影响）
- Playwright 实测三页：scrollWidth 全部回到 1280, 纵向滚动正常

---

# 第十四轮（2026-08-30 深夜）：全量扫描落地 + 视频逐幕折叠参数 + 分析评论 + 图表/UI 视觉统一

## 1：老视频翻红监测——全量扫描（已完成并实跑验证）

- 确认原状：每轮仅采近 45 天活跃窗口(约 40 条)，153 个老视频只有入库基线，翻红不可见
- 新增 `monitor.full_sweep_days`(默认 3)：到期轮对所有入库视频全量采集
  (`full_sweep_due` 纯函数 + accounts.last_full_sweep 落库)；
  实跑一轮：原神 81 / 星铁 79 / 绝区零 全量快照入库，视频 193→230(深回填持续推进)
- 存储影响可忽略(~0.1MB/轮)；采集分层：日常 7 分钟活跃窗 + 每 3 天约 20-40 分钟全量

## 2：视频逐幕折叠参数面板（GUI，已完成）

- 「视频报告」页七幕各为 `<details>` 折叠模块，展开即编辑该幕全部展示参数：
  片头(文案/字号/环形文字/色块开关)、总览(分游戏行数)、播放走势(Top/线宽/
  快照点开关/纵轴下缓冲/标签折行宽)、净增量·视频(Top/线宽/零基线开关)、
  净增量·分游戏、竞跑(Top/截断阈值/条形高度)、片尾(结束语)
- 表单序列化为 opts JSON 写 `data/gui_video_opts.json`，子进程 `--opts-file` 读取；
  `video_fluid` 绘制函数全面读参(`_o()` 助手)；collect 加池化参数支持 Top>8

## 3：数据中心修复 + 人工分析评论（已完成）

- 修复小时分布 hover 变量冲突、轮次卡显示真实总轮数
- 新增「分析评论」卡：读取 `data/insight.md`(助手人工撰写, 非自动生成)，
  带更新时间标注；已按当前真实数据写初版(整体判断/关键发现/数据质量提示/观察建议)

## 4：图表重绘为纸感平设（已完成）

- matplotlib 图表全套换纸质底(#f7f6f0)+墨色文字+平涂三色(珊瑚/奶油/雾紫)：
  编辑部报头(珊瑚角块+粗标题+细分割线+右侧 mono 时期)、点线网格、
  平涂柱白描边、Top 榜 mono 名次、编辑部子图标题(角块+左对齐)；
  index.html 索引页同步纸感；与 UI/视频色系完全统一

## 5：UI 内容块 MAD 设计层（已完成）

- 卡片按序轮换强调色(--acc 珊瑚/奶油/雾紫/薄荷)：标题菱形、左上角标、
  右上空心圆角标(悬停呼吸)全部随色
- 页标题波浪分隔线(SVG 曲线)；背景加大四分之一圆弧/点弧/实色浮点
- 数据条/分布条/比例条加载生长动效；表格行悬停左侧色条；账号点悬停脉动

---

# 第十五轮（2026-09-12）：时间轴端点修正 + 新增「净增量·近期视频」幕

## 1：时间轴端点修正（已完成）

- 新增 `_timeline_ticks`：两端端点=起始时间与末时间（必定标注，标签向轴线内对齐
  不越出），内部逐日刻度距两端/相邻标注 ≥24h 才保留，消除端点处重复与拥挤
- 总览/竞跑/走势三处时间轴统一接入

## 2：新增「净增量·近期视频」幕（已完成）

- 场景表 SCENES_FLUID 在净增量·视频之后插入 gains_recent(默认 8s)；
  仅收录发布于 数据跨度×2 内的视频(如跨度一周→两周内发布)，过滤老视频
- collect 提供 trend_all 未截断池 + 视频发布时间(created_ts)；
  `_trend_like` 重构支持 scene/trend_items/header 定制复用
- GUI 折叠面板新增该幕(时长/Top/线宽/折行宽)；CLI --scene 名称同步
- 实渲染验证(57.50s/8幕)：新幕正确命中近期视频、时间轴端点正确

---

# 第十六轮（2026-09-12）：时间轴均匀日格 + 近期幕兜底 + 标签瀑布防叠印

## 1：时间轴按日期均匀分格（已完成）

- 轴域归一化到自然日边界(起点日 00:00 → 末.session 日次日 00:00)：
  每个日期恰好占一格, "9/4→9/6 恰好两格"；曲线/扫描/进度按真实时间映射不受影响
- 9-11 重复标签消除(端点=起末日期, 末端边界只留刻度不标注)；
  端点标签向轴线内对齐, 内部长跨度按 k 格均匀抽稀

## 2：净增量·近期视频幕兜底（已完成）

- `_recent_filter`：目标范围(跨度×2)不足 top_n 时, 按发布时间从近到远
  继续回溯补满; 副标题明示"不足 N 个已兜底回溯至 xx-xx · 命中 M 个"

## 3：右侧标签瀑布防叠印（已完成, 修复渲染中发现的叠印）

- 原防重叠在贴底处失效(多个近值标签被钳回同一 y 叠印)
- `_side_timeline` 重写为自下而上瀑布分配(值最小者贴底先放, 依次向上,
  gap 随标签数自适应), 再自顶向下收紧不超顶; classic 共享同函数受益

## 4：验证

- 单测 18/18(_time_axis 日域/日格均匀/兜底回溯); GUI 同路径实渲染 57.50s
  完整, 抽帧确认三处时间轴均匀无重复、近期幕 8 个近期视频、标签无叠印

---

# 第十七轮（2026-09-12）：时间轴右端=末时间, 其后无空白

- 用户反馈: 折线图 09-11 右侧仍有空白(前版轴域补到了次日 00:00)
- `_time_axis` 改为 [起始日 00:00, 末时间本身]: 内部日期格仍均匀(1格/日),
  右边缘恰好落在最后一个数据时刻(09-11 21:30), 其后无空白
- `_day_cells`: 与末时间同日的 0 点网格线保留但不标注(该日期由末端承担),
  末端标注向内对齐; 总览/竞跑/走势三处统一
- 实渲染 57.50s 验证: 三处时间轴左端 09-04、右端 09-11 贴边, 均匀无重复

---

# 第十八轮（2026-09-12）：末同日格线删除 + 折线从轴起点开始

## 1：时间轴末同日 0 点格线删除（已完成）

- 用户指出 09-10 与 09-11 标注间有两格(末同日 0 点多出一根格线)
- `_day_cells` 删去末时间所在日的 0 点刻度: 09-10 之间到末端 09-11 只有一格;
  该日期由末端标注承担, 无重复

## 2：折线从轴起点开始（已完成）

- 用户要求: 规定从 9/4 开始, 折线就应从 9/4 画起——哪怕数值是零贴着坐标轴
- `_trend_like` 曲线改为从轴起点 t0 采样: 首快照之前,
  净增量幕贴轴为零(与轴线重合), 播放量走势幕平推首值;
  到各视频首个快照时刻再"恢复到正常数值"
- 附带修复: 渲染验证中发现 `_5.mp4` 一次偶发未收尾(54.67s)为环境因素,
  重渲染 57.50s 完整

---

# 第十九轮（2026-09-12）：时间轴重构为严格"一天一格"

## 用户问题

09-10 到 09-11 之间仍占两格长度(旧域末端补到 09-11 21:30, 该日占了 1.9 格);
要求严格"每天对应一格的长度", 时间轴到 09-11 为止。

## 方案: 日索引坐标系(已完成, 实渲染验证)

- `_time_axis` 返回 (起始日 date, 日格数 nd); 轴位置 x ∈ [0, nd-1],
  **每个日期恰好一格**, 末端(09-11)就是轴最右端, 其后没有任何空白
- 全部八幕(总览时间轴/播放走势/净增量两幕/分游戏/竞跑)切换到日索引坐标:
  快照按所在日落入对应格位(同日多快照取后者), 相邻日期间距恒等于一格
- 折线从轴起点(第一天)开始: 净增量幕首快照前贴轴为零, 播放量幕平推首值
- 新增 `_side_timeline_d`(瀑布防叠印, 日索引)替换 fluid 对 video.py
  `_side_timeline` 的依赖; 分游戏幕合计曲线改为按日索引重算(不受 classic 影响)
- tests 18/18; GUI 同路径实渲染 57.50s, 抽帧确认三处时间轴均匀且末端正确

---

# 第二十轮（2026-09-12）：修复右侧增长数值不随扫描跳字

- 根因: 日索引重构时 `_side_timeline_d` 的动画推进分母误写为
  `span - side_pad`(日格数7 - 定格帧数75 = 负, 钳为1), 扫描位置从第1帧起
  即跳到终点 → 右侧数值全程冻结在最终值
- 修复: 分母改回按帧数 `n_frames - side_pad`(与折线扫描线同步)
- 实渲染验证: 同一支视频 11s/15s/18.5s 三帧, 右侧数值 1452.5万 → 1453.x →
  1454.9万 连续跳字至最终值, 位置随折线端点同步

---

# 第二十一轮（2026-09-12）：Top10 化 + 竞跑近期化 + 账号对比 + 新增星布谷地

## 1：所有收录数 8 → 10（已完成）

- collect 池默认 trend_pool 10；走势/净增量·视频/净增量·近期三幕 Top 默认 10；
  GUI 占位符同步；classic 同步受益

## 2：竞跑改为"近期发布的视频"（已完成）

- 播放量竞跑不再从全量池取 Top10：与净增量·近期同规则——
  仅收录发布于 数据跨度×2 内的视频按当前播放量取前十,
  不足则按发布时间从近到远回溯补满十个
- 实渲染验证: 八日跨度下 Top10 全部为 09-05 后发布(部分兜底回溯)的近期视频

## 3：账号对比(已完成)

- `collect` 支持 only_mids 过滤; 视频 opts 增加 mids;
  GUI「视频报告」页新增"对比账号"多选(不选=全部), 整支视频仅基于所选账号数据;
  CLI `--mids 401742377,1636034895` 等效
- 实渲染验证(仅星铁+绝区零): 总览/竞跑/图例均只含两账号 ✓

## 4：新增星布谷地官号（已完成）

- mid 3546622923377024(card API 核验, 95.3万粉)已加入配置并首采
  (7 个视频全部入库带快照); 后续每日自动采集

## 5：附带修复

- GUI 多进程僵尸(多轮重启累积, 抢占 8322 致 POST 打到旧代码)已清理;
  提醒: 重启 GUI 前先 TaskStop 旧后台任务

---

# 第二十二轮（2026-09-12）：竞跑 Top38 修复（回归测试覆盖）

- 用户报告竞跑变 Top38: `_recent_filter` 命中分支漏了 `[:top_n]` 截断——
  跨度×2 内有 38 个近期视频时全部返回
- 修复截断; 新增回归测试(38 条池断言返回 10 条按期末播放降序)
- 实渲染验证: 竞跑定格帧 "播放量竞跑 Top10" 恰十条、时间轴末端正确

---

# 第二十三轮（2026-09-12）：折线扫描缓动 + BGM 踩点合成

## 1：折线/竞跑扫描非线性缓动（已完成）

- 播放走势/净增量两幕/分游戏/竞跑的扫描推进全部加入
  先快后慢缓动(_ease 三次缓出), 侧标签位置与数值跳字同步缓动,
  竞跑的定格段保持不变——告别匀速观望感

## 2：BGM 踩点合成（新功能, 已完成并实渲染验证）

- 新增 `bmon/bgm.py`: 每首 BGM 分析一次并缓存标记
  (`bgm/<文件名>.beats.json`, 按文件大小+修改时间失效, 免重复计算);
  节拍检测优先 librosa, 不可用时回退能量包络峰值(纯 numpy)
- 生成视频时: 各幕边界按权重铺在 BGM 时长上并**吸附到最近的节拍**
  (每幕不短于 2.2s), 分镜切换踩在点上; 视频总时长跟随 BGM
- 渲染完成后 ffmpeg 合成音轨(裁剪至视频长 + 末端 1.2s 淡出)
- BGM 库目录 `bgm/`(放入 mp3/wav/flac/m4a/ogg 即可在 GUI 下拉选择);
  CLI `--bgm 路径`; 依赖 librosa 已加入 requirements
- 验证: 33s 测试 BGM → 输出 33.02s + 音轨存在,
  分镜边界 [0, 2.2, 5.46, 11.2, 16.37, 20.96, 25.55, 30.82, 33.02] 全部落节拍

## 3：附带修复

- 清理 9 个僵尸 GUI python 进程(多轮重启累积抢占 8322 端口, 导致 POST
  打到旧代码——此前若干"改了没生效"均源于此)
